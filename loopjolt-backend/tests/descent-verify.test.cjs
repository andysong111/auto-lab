const {test}=require('node:test'),assert=require('node:assert/strict');
const descent=require('../../vibe-arcade/descent/core.js'),challengers=require('../../vibe-arcade/challengers/core.js'),arcade=require('../../vibe-arcade/arcade3/core.js'),orbit=require('../../vibe-arcade/games/orbit-sprint/core.js'),verify=require('../verify-run.js');
function noInput(r,game){const s=game?r.create(game,7):r.create(7);while(s.alive)r.step(s);return {s,body:{actions:[],ticks:s.tick,score:49999,country:'US',game:'other'}};}
test('new v2 replay is routed by server-owned version, not body fields',()=>{const {s,body}=noInput(descent);const v=verify({game:'gyro-drop',version:descent.VERSION,seed:7},body,orbit,arcade,challengers,descent);assert.equal(v.score,s.score);assert.equal(v.version,descent.VERSION);assert.equal(v.score,0);});
test('in-flight v1 runs remain valid and isolated',()=>{const {s,body}=noInput(challengers,'gyro-drop');const v=verify({game:'gyro-drop',version:'gd-phaser-v1',seed:7},body,orbit,arcade,challengers,descent);assert.equal(v.score,s.score);assert.equal(v.version,'gd-phaser-v1');});
test('a nonexistent version is rejected',()=>{assert.throws(()=>verify({game:'gyro-drop',version:'gd-fake',seed:7},{actions:[],ticks:90},orbit,arcade,challengers,descent),/version_mismatch/);});
test('other games retain their original verification adapter',()=>{for(const g of ['core-pins','nova-merge']){const {s,body}=noInput(challengers,g);const v=verify({game:g,version:challengers.CONFIG[g].version,seed:7},body,orbit,arcade,challengers,descent);assert.equal(v.score,s.score);}});
