# Keywake same-ID depth polish

IN PROGRESS — owner review HOLD. GAME-20260923-141, original v3 READY_TO_SHIP superseded by `owner_difficulty_too_low`: “게임이 너무 단순하고 너무 쉽다.” Production remains unauthorized; no main merge or production publish.

Base infrastructure: main `feeed4ffde39872714f473803a0158ba3700fb08`. Original exact v3 RC: `90bfac0349e4219e3e28f37ac948371afff98157`, Draft PR #56. Prior audit remains preserved in closed-unmerged PR #55, evidence commit `58991a5d2b0cfbd0f83bc8fae42fd60d445da75c`.

## Budget and provenance

- Same GAME ID. Current lifetime repairs2/5 and real provider calls3/6; next operation is `GAME-20260923-141/repair/3`, targeting v4. No rebuild, clone or reset.
- gpt-5.6-terra, concurrency1, maximum3 further calls. Existing conservative lifetime estimated ceiling USD2 stays in force. Prior estimated cost USD0.386828; usage-based estimate, not billed reconciliation.
- Restore exact release checkpoint artifact10729540989, ZIP SHA256 `6c02775dde2bbd2e906e375f7dd6b3ca88254e0064c9703709f75b2c67288157`. Verify original ledger, manifest and source hashes before any call. Every prior operation remains byte-identical as data.
- Provider-owned candidate source only. Work authored trusted specification, finite DATA oracle, owner-observation intake and evidence transport, never candidate implementation or executable candidate QA.
- The original60-minute wall budget would otherwise expire while awaiting owner review. A validated closed owner-review interval begins at the recorded v3 READY transition and ends on authorized continuation. Original `started_at`, prior active time, operations, money, tokens, calls and repair attempts remain intact. Lifetime active execution remains capped at60 minutes. Invalid/open/future/overlapping/unapproved pauses fail closed. This is explicit owner continuation, not a retry of an ambiguous operation.

## Revised design and measurement

Six distinct 4×4 key/gate route packs, selected deterministically for all uint32 seeds by the documented bounded hash. The six reviewed seeds cover every pack. Layouts are independently searched offline and then fixed as data; runtime generation does no search. Walls/edges, key cells, gate constraints and optimal inputs differ; topology comparison excludes rotation/mirror duplicates.

Room1 has one key and conditional shortcut choices. Room2 makes A/B key order consequential. Room3 combines A/B ordering, multiple gated routes and a C key that requires A+B. Choosing the inferior key order adds at least4 moves. All reachable oracle states retain a completion path.

Independent shortest required inputs meet6/10/14 minima and strictly increase. Consequential route decisions are3/4/at least5, also strictly increasing. A decision is a route state with at least two viable non-backtracking alternatives of different remaining optimal cost; this is not merely corridor length.

Generic Product Gate v1 is unchanged. Its `complexity` is actual immediate legal width, bounded by four direction controls, so entry widths remain2/3/4. These are deliberately distinguished from room-wide consequential decision counts. Oracle bounds remain128 nodes per seed and64 total normal inputs including branch-return probes. Projection stays stage/cell/key_mask/outcome. Gate cross-checks real keyboard/touch input and deterministic state transitions for all six seeds.

Score is awarded only when clearing a room, with detours reducing the award relative to independently computed par. Keys, gates, blocked steps, waiting and reversible movements award nothing. Same-seed replay supports route improvement and Perfect/Efficient/Detour ratings. The40-second timer and directional controls remain; no speed/HP/damage difficulty shortcut.

Requested presentation distinguishes key pickup, gate unlock, room clear, hatch open and final clear; reduced-motion alternatives and proven v3 lifecycle/Preview asset-base handling remain required.

## Preflight and pending evidence

See `commissioning/keywake-depth-polish/oracle-review.json`, `layouts.json`, `proposal.json` and `preflight-tests.log`. Trusted design/continuation/provider preflight:40 tests passed. Dry-run owner intake preserves source and all three prior operation IDs; next compiled request fits the existing input reservation limit.

Actual candidate conformance, v4/v5/v6 changes, token/cost usage, scoring/feedback evidence, screenshots/video, exact Preview, final state and remaining repair budget will be appended after bounded execution. Owner fun/depth review remains required even if automated gates pass. Production authorization stays false.
