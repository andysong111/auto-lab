# Ribbon Shift terminal evidence

`GAME-20260922-131` remains **REJECTED v6, repair5/5**. Start with [the complete handoff](../../HANDOFF_WORK_THIRD_REAL_GAME.md).

- `manifest.json`: authoritative rejected Factory state.
- `commissioning-summary.json`: six real operations, tokens, USD0.644552 estimate.
- `provider-ledger.json`: archived Worker ledger; source/response IDs, no credentials.
- `provider-requests/`: exact compiled build and repair inputs.
- `source/v1..v6/`: byte-preserving `.txt` source copies, never executable routes.
- `source-provenance.json`: real provider-output identity, zero manual edits.
- `provider-feedback-audit.json`: no actual validation failures; conditional quarantine covered by tests.
- `factory/`: original build/repair/QA history, phase screenshots and videos. Generic terminal files retain `viewport-N.png` names.
- `difficulty.json`: six seeds, independent minimum shifts and actual action counts.
- `motion-audit-initial/`: earlier narrow diagnostic, no full product suite.
- `motion-audit-final/`, `product-audit-final/`: verified read-only run35734988459; diagnostic PASS, original REJECTED unchanged.
- `release-review-packet.json`: expected `passed:false`; no RC/Preview/production authorization.
- `final-contracts.log`, `final-worker.log`:43 and55 contract tests PASS.
- `github-verification.json`: tested system-head workflows, no RC claim.
- `production-regression.json`, `baseline-*.png`: existing-game baseline evidence.
- `integrity.json`: SHA256 file index, excluding itself.

Do not modify or reset original source, ledger, manifest, QA or rejected history. The final audit preserves their hashes. Committed evidence remains after Actions artifact expiry. No API credentials or temporary signed URLs are retained.
