# Third real-game commissioning — Ribbon Shift

One new candidate GAME-20260922-131, based on main7e8e260ab1895eeec845ab216408820f5f444afe. Prism and Cloud Cargo remain terminal rejected; no source or ID reuse. Cloud Cargo is a provider-contract failure, not evidence against its whole mechanic family.

Run check-spec on proposal.json before any paid call. The branch-scoped one-shot workflow uses the existing Worker, AI adapters, Responses provider, Factory and Docker QA. Only the copied dedicated policy enables intake. Production, release, marketing switches remain off for generation. Successful Factory run stops RC_READY for independent visual review and subsequent exact-source RC.

Model gpt-5.6-terra; existing OPENAI_API_KEY is injected only into the trusted provider step. User authorizes calls below KRW50,000. This commissioning retains the stricter checked-in USD2 estimated ceiling (below user USD3), calls6, repairs5, concurrency1,60min elapsed. Standard official pricing rechecked2026-09-22: https://developers.openai.com/api/docs/pricing ; conservative long-context4/18 USD per1M input/output, no cache discount; billed cost unknown.

A trusted beforeProvider hook checks the durable ledger BEFORE reservation. Two consecutive identical validation errors cause PAUSED_PROVIDER. Preserve the entire state; inspect the precise errors, quarantine and compiled requests. Continuation requires a review file matching the exact pair fingerprint with substantive system findings. It never resets ledger, time or repairs. No workflow rerun from empty state is safe after any actual call.

Compiled requests are saved privately for proving source/error feedback. Rejected provider output is data only; latest main's validator/quarantine/compiler remain authoritative. The optional browser product selector uses the same isolated Docker boundary, a separate300s product deadline and unchanged150s common hard gates. Candidate scripts never run on the host. Every generated edit must match a provider COMPLETE response; Work writes only trusted specs, QA and orchestration.

Tests: npm test --prefix autonomy; npm run test:worker --prefix autonomy. The dedicated workflow checks these gates before the secret step. Independent reverse BFS proves shortest forward shift counts2/3/4; actual Chromium uses keyboard/CDP touch for six seeds, checks early UI/preference failures independently, capture safety and fresh local-practice best persistence. Audio is intentionally absent. Screenshots/video require subsequent human-agent visual inspection; no automatic fun score or parity claim.

Rollback is stop/close the commissioning PR, keep all state and terminal history. Nothing is merged/published to production. Full final outcome and links will be in HANDOFF_WORK_THIRD_REAL_GAME.md.
