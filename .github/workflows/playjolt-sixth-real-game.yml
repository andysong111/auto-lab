name: Sixth real candidate — archived terminal record
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Terminal candidate notice
        run: |
          echo "GAME-20260923-161 / Ember Choir is terminal REJECTED at repair 5/5."
          echo "This workflow is archived. It performs no provider call, repair reset, production release, or candidate revival."
